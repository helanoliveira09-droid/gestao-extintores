# URL de conexão do MongoDB Atlas (banco de dados em nuvem)
# Exemplo: mongodb+srv://usuario:senha@cluster0.xxxxx.mongodb.net/gestao-extintores?retryWrites=true&w=majority
MONGODB_URI=

# Porta em que o servidor irá rodar localmente (o Render define isso automaticamente em produção)
PORT=3000
